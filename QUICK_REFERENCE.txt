# 🚀 Quick Reference Card

## Start Your Set Up in 3 Steps

```bash
# Step 1: Fix Docker (if needed)
sudo usermod -aG docker $USER && newgrp docker

# Step 2: Start Everything
cd "/media/rageeb-hasan-shafee/New Volume/3-2/CSE 326/Daraz"
docker compose up -d

# Step 3: Verify
curl http://localhost:4000/health
```

---

## Database Info

**Host**: localhost (or `db` in Docker)  
**User**: daraz_user  
**Password**: daraz_password  
**Database**: daraz_db  
**Port**: 5432  

**Tables**: 7 (users, categories, products, carts, cart_items, orders, order_items)  
**Records**: 100+ test records  

---

## Documentation Map

```
Start → README_SETUP.md (overview of everything)
        ↓
      DATABASE_SETUP.md (how to set up)
        ↓
      CHANGES.md (what changed)
        ↓
      SQL_REFERENCE.md (copy-paste queries)
        ↓
      DEVELOPMENT_CHECKLIST.md (what to build next)
```

---

## Key Database Queries

```sql
-- Connect
psql -h localhost -U daraz_user -d daraz_db

-- View all tables
\dt

-- See sample data
SELECT * FROM products LIMIT 5;
SELECT * FROM users;
SELECT COUNT(*) FROM orders;
```

---

## What's Ready

✅ PostgreSQL Schema (7 tables)  
✅ Sample Data (100+ records)  
✅ Docker Configuration  
✅ Node.js DB Connection  
✅ Documentation (5 guides)  
✅ SQL Examples (40+ queries)  
✅ Development Roadmap  

---

## What's Next

1. Test database connection
2. Implement user authentication
3. Build product listing API
4. Create shopping cart
5. Process orders

See DEVELOPMENT_CHECKLIST.md for detailed phases!

---

## File Changes Summary

| File | Change | Impact |
|------|--------|--------|
| migrations.sql | ✨ Complete rewrite | 7 modern tables |
| seed.sql | ✨ Complete rewrite | 100+ test records |
| docker-compose.yaml | ✨ Removed version | Fix deprecation |
| .env.example | 🆕 Created | Config template |
| 5 Documentation Files | 🆕 Created | Comprehensive guides |

---

## Database Diagram

```
Users (UUID)
  ├─ Carts
  │   └─ CartItems → Products
  │
  └─ Orders (UUID)
      └─ OrderItems → Products

Categories → Products (with discounts & flash sales)
```

---

## API Ready (After Implementation)

```
POST   /api/users/register           (User signup)
POST   /api/users/login              (User login)
GET    /api/products?category=...    (Products with filters)
POST   /api/cart/add                 (Add to cart)
POST   /api/orders/create            (Create order)
GET    /api/orders                   (Order history)
```

---

## Testing Your Setup

```bash
# Database health
psql -h localhost -U daraz_user -d daraz_db -c "SELECT COUNT(*) FROM products;"
# Should return: 14

# API health
curl http://localhost:4000/health
# Should return: { status: 'success', db_time: ... }

# Docker status
docker compose ps
# Should show: db and app containers running
```

---

## Important Security Notes

🔒 **Passwords in seed.sql are for testing only**

When building authentication:
- Use bcrypt to hash passwords
- Implement JWT tokens
- Add CSRF protection
- Validate all inputs
- Use HTTPS in production

---

## Useful Docker Commands

```bash
# Start containers
docker compose up -d

# Stop containers
docker compose down

# View logs
docker compose logs -f

# Connect to database
docker exec -it daraz_postgres psql -U daraz_user -d daraz_db

# Restart containers
docker compose restart
```

---

## Need Help?

📚 **Documentation Files** (all in project root):
- README_SETUP.md - Overview
- DATABASE_SETUP.md - Installation guide  
- CHANGES.md - What changed
- SQL_REFERENCE.md - Query examples
- DEVELOPMENT_CHECKLIST.md - Your roadmap

---

**Status**: ✅ READY TO CODE  
**Date**: March 18, 2026  
**Next**: Implement Phase 3 - User Authentication
